class Controllers {
  var vessel;
  var line;
  var timer;
  
  function initialize(vessel_, line_, timer_) {
  	vessel = vessel_; // VesselController
  	line = line_; // LineController
  	timer = timer_; // StartTimerController
   }
}